export default async function AccessPending({searchParams}:{searchParams:Promise<{gate?:string}>}){const {gate}=await searchParams;return <main className='gateLayout'><section className='card'><span className='status'>IDENTITY PROVIDER CONNECTION REQUIRED</span><h1>Secure sign-in endpoint is not configured</h1><p>The {gate==='teacher'?'teacher/faculty':'student'} portal gate is deployed, but the live SSO entrypoint must be supplied through environment configuration before authentication can complete.</p><p className='muted'>Required environment variable: {gate==='teacher'?'NEXT_PUBLIC_STAFF_SSO_ENTRYPOINT':'NEXT_PUBLIC_STUDENT_SSO_ENTRYPOINT'}. No credentials or secrets belong in Git.</p><a className='button gold' href='/signin'>Return to sign-in</a></section></main>}